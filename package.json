{
  "name": "notdownhub-monorepo",
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "docs:style": "node scripts/doc-style-check.mjs",
    "dev:web": "pnpm --filter @notdownhub/web dev"
  }
}
